services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: gymcourse
      POSTGRES_PASSWORD: gymcourse
      POSTGRES_DB: gymcourse
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: